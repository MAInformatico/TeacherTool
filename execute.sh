
// This script is used to run the FastAPI application.
// It can be executed in a terminal or command line interface.
// If you want to run the FastAPI application, you can use the following command:
fastapi run fastapi_main.py


// If you want to execute the application in local mode, using the terminal or command line interface, you can use the following command:
python main.py
